Perform a DiffPal CI code review of the task snapshot. Inspect the requested Git diff and relevant nearby code before producing final JSON. Produce structured findings only for discrete, actionable, patch-introduced or patch-worsened issues the pull request author would likely fix before merging. A clean result is valid only after inspecting the changed files and finding no qualifying issues.
